<h1>Demo#index</h1>
<p>Hello from the index page</p>

<%= lin %>k_to('Go back to hello page', {:action => 'hello', :id => 20, :page => 5})
<%= lin %>k_to('Go back to hello page', {:action => 'hello', :id => 20, :page => 5})
